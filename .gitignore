# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build

# make sure to not include package-lock.json because react needs to see that to properly deploy the app and I did not know this until now :p
# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
